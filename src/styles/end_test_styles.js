import styled from "styled-components";

export const MainComponentEndTest = styled.div`
  max-width: 1440px;
  width: 100%;
  display: flex;
  justify-content: center;
  gap: 4rem;
`;
export const EndTestBox = styled.div`
  display: grid;
`;
export const EndTestBoxText = styled.p`
  color: var(--color-icon);
  text-align: center;
  letter-spacing: -0.05em;
  font-size: 0.8rem;
  font-weight: 400;
`;
export const EndTestBoxResultText = styled.p`
  color: var(--color-text);
  font-size: 3rem;
  font-weight: 700;
  letter-spacing: -0.1em;
`;
